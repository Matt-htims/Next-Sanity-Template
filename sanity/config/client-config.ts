import {
	sanityProjectId,
	sanityDataset,
	sanityApiVersion,
} from '@/sanityConfig';

const config = {
	projectId: sanityProjectId,
	dataset: sanityDataset,
	apiVersion: sanityApiVersion,
	useCdn: false,
};

export default config;
